GEMINI_API_KEY=

# "groq" にするとプロトタイプ用（無料枠のレート制限が緩い）に切り替わる。未設定/"gemini"なら本番用のGemini。
LLM_PROVIDER=gemini
GROQ_API_KEY=

# 未設定でも動作するが、匿名枠は1日あたりのクォータが低いため書誌概要の補完が失敗しやすい。
# Google Cloud Console で「Books API」を有効化してAPIキーを発行すると、無料枠のクォータが増える。
GOOGLE_BOOKS_API_KEY=
